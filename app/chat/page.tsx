import { ChatShell } from "@/components/chat/chat-shell";

/** A fresh chat. The conversation record is created on the first message. */
export default function NewChatPage() {
  return <ChatShell />;
}
